# 指数操作符

**ECMAScript7** 新增了 **指数操作符**, `Math.pow()` 现在有了自己的操作付`**`,结果是一样的:

```javascript
console.log(Math.pow(3, 2)); //9;
console.log(3 ** 2); //9;
```

```javascript
console.log(Math.pow(16, 0.5)); //4;
console.log(16 ** 0.5); //4;
```
