# 光栅化

`光栅化` 是指将网页中的矢量图形、文本和其他可见元素转换为屏幕上的像素的过程。

在网页渲染过程中，光栅化是绘制阶段的一部分，它将绘制树中的每个元素转换为屏幕上的实际像素点，从而呈现给用户可见的图像。

浏览器将矢量图形、文本和其他元素的具体颜色、边框和阴影等细节转换为像素阵列。这些像素最终在屏幕上形成可见的图像。

![光栅化](./images/Snipaste_2024-07-26_00-51-11.png)
