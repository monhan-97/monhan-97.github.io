# HTTP 版本

## HTTP 1.0

- 包含 `GET`、 `POST`、 `HEAD`请求方法

### 缺点

- 每次`请求`或者`响应`都需要建立关闭一个独立的`TCP链接`。增加了网络延迟和开销。
- 不支持长连接的机制,导致频繁的`TCP链接`建立和关闭,影响性能。
- 缺少完善的`缓存机制`和`缓存策略`
- 一个`TCP链接`不能同时处理多个请求,造成请求处理的顺序依赖于响应的完成。也就是**队头堵塞**的原因

## HTTP 1.1

## HTTP 2.0
